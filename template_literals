In the ES6 version of JavaScript, we can insert, or interpolate, variables into strings using template literals. 

const myName = 'Teddy Huang';
let myCity = 'Chaoshan'
console.log(`My name is ${myName}. My favorite city is ${myCity}.`);
